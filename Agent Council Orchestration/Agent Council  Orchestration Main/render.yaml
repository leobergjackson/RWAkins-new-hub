services:
  - type: web
    name: aerofyta-agent
    runtime: docker
    dockerfilePath: ./agent/Dockerfile
    dockerContext: ./agent
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
